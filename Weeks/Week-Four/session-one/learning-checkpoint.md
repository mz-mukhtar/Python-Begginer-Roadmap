# ✅ Learning Checkpoint

Before continuing, make sure you can:

- [ ] Index and slice strings
- [ ] Format text using `.upper()`, `.lower()`, `.strip()`
- [ ] Validate string content using `.isdigit()` and `.isalpha()`
