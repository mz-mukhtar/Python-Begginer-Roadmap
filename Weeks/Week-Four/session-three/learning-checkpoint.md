# ✅ Learning Checkpoint

Before continuing, make sure you can:

- [ ] Save and load JSON data using `json.dump()` and `json.load()`
- [ ] Read and write CSV records using the `csv` module
- [ ] Build programs that persist data across restarts
