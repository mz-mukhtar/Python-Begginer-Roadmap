# ✅ Learning Checkpoint

Before continuing, make sure you can:

- [ ] Open files in read (`"r"`), write (`"w"`), and append (`"a"`) modes
- [ ] Use `with open(...)` safely
- [ ] Inspect relative file paths
