# 🚀 Session Challenge

## Production-Ready Student Model

### Description

Build a production-ready `Student` class with full validation, string formatting (`__str__`), dictionary serialization (`to_dict`), and deserialization (`from_dict`).

Try to complete the challenge before opening the solution.
