# 🚀 Session Challenge

## Course Registration Class

### Description

Create a `Course` class (`code`, `title`, `capacity`, and `enrolled_students` list) with methods `enroll(student_name)` and `is_full()`.

Try to complete the challenge before opening the solution.
