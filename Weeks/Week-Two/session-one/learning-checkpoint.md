# ✅ Learning Checkpoint

Before continuing, make sure you can:

- [ ] Write a `while` loop with an exit condition
- [ ] Use a `for` loop with `range()`
- [ ] Use `break` to exit a loop immediately
- [ ] Use `continue` to skip an iteration step

## Ready to Continue

You are ready when you can complete most items without copying the lesson code.
